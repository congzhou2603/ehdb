import React from 'react';
import { GalleryModel } from "../../model/gallery";

export default function GridItem(prop: { data: GalleryModel, columnCount: number }) {
    return (
        <div >
            
        </div>
    )
}